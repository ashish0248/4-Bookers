<body>
	<% flash.each do |message_type, message| %>
　	<p class="alart"><%= message %></p>
	<% end %>
	<h1>Books</h1>
	<table>
	 	<thead>
			<tr>
				<th>Title</th>
				<th>body</th>
				<th></th>
				<th></th>
				<th></th>
			</tr>
		 </thead>
		 <tbody>
			<tr>
				<% @books.each do |book| %>
					<th><%= book.title %></th>
					<th><%= book.body %></th>
					<th> <%= link_to 'Show',book_path(book.id) %></th>
					<th><%= link_to 'Edit', edit_book_path(book.id) %></th>
					<th><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure?" %></th>
				
			</tr>
			<% end %>
		 </tbody>
	</table>

	<h2>New book</h2>
	<%= form_for(@book, url:"/books") do |f| %>
			<% if @book.errors.any? %>
				<div class="alart-message">
					 <h3 class="alart-body"><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h3>
    			<ul>
     				 <% @book.errors.full_messages.each do |message| %>
       					 <li><%= message %></li>
     				 <% end %>
    			</ul>
    			</div>
		<% end %>

		<div class="field">
			<label for = "book_title">Title</label>
			<%= f.text_field :title, id: "book_title" %>
		</div>
		<div class="field">
			<label for = "book_body">Body</label>
			<%= f.text_area :body, id: "book_body" %>
		</div>
			<%= f.submit 'Create Book' %> 
	<% end %>

</body>
